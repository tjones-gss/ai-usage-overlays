# Runtime files — generated at startup, not tracked
*.pid
*-state.json
*-error.log
*-cache.json
overlay-history.json
# Holds a live Anthropic OAuth token - must never be committed.
claude-profile.json
# Local mirror of WSL transcripts/sessions (hundreds of MB of cached JSONL).
wsl-mirror/
.codegraph/
.worktrees
dist/
packaging/build/
